{
  "name": "x402-mcp",
  "private": true,
  "scripts": {
    "build": "cd dashboard && pnpm install && pnpm run build && cd .. && node -e \"require('fs').cpSync('dashboard/dist', 'dist', {recursive: true})\""
  }
}
